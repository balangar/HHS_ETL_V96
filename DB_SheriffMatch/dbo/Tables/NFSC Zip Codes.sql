CREATE TABLE [dbo].[NFSC Zip Codes] (
    [_ID_]       INT          NOT NULL,
    [_Zip_Code_] VARCHAR (50) NOT NULL,
    [_NFSC_]     VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_NFSC Zip Codes] PRIMARY KEY CLUSTERED ([_ID_] ASC)
);

